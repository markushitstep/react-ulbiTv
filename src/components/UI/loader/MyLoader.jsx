import React from 'react'
import style from './MyLoader.module.css'

const MyLoader = ({posts, title, remove}) => {

    return (
        <div className={style.loader}>
            
        </div>
    )
}

export default MyLoader;
